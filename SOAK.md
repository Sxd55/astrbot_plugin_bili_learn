# Bili Learn 实机测试流程（v1.11.1）

本轮新增：**模型调用策略**（任务分档、Token 预算闸、拒答重试、按任务记账）。
上轮修复：`/bilearn once` 完成后误报"未运行"、同名文档复用不计配额、脏配置崩任务（cron/间隔/数字守卫）、后台任务堆积、
全角逗号关键词（`科技，数码`不再变成一个词）、`subtitle_page_limit=0` 真读全部 P。

## 0. 更新与加载

1. **更新方式（重要）**：AstrBot 上传 zip 时若同名目录已存在会直接拒绝安装。
   正确流程：WebUI → 插件 → Bili Learn → **卸载**（不要勾「删除配置」「删除数据」）
   → 再上传新 zip 安装 → 重载。
2. 前置：AstrBot 配好 Embedding Provider，并在聊天配置里把 `Bili Learn` 知识库绑到会话，
   否则只写本地摘要库、语义检索不可用（回退本地关键词匹配，属正常）。
3. 加载日志期望：`Bili Learn loaded`；无 Embedding 时有一行 KB 未创建警告（正常，
   配好后重载即建库）；定时已注册（`daily cron registered at 01:00`，按配置时间）。
4. 已知环境限制：**部分网络出口会被 B 站 view 接口 412 拦截**（搜索正常、详情 412）。
   表现为整轮 `aborted=rate_limited`、health 页冷却秒数 > 0。这是 B 站风控，
   不是插件 bug；换个网络（家庭宽带/手机热点）再测。如 view 正常请忽略本条。

## 1. 模型调用策略（本轮重点）

1. **任务分档**：设置里给「精准模型」和「快速模型」各选一个（精准用强模型、快速用便宜模型），
   `/bilearn once` 跑一轮 → 面板「今日 Token」有数；`/bilearn status` 出现
   `今日Token=…（硬限 … / 软限 … / 预算跳过 … 次）`。
   若面板/日志显示走了不同 Provider，说明摘要走精准、复审审核走快速。
2. **显式优先**：再把「摘要模型」手动选一个 → 重跑一轮。
   期望：摘要用你显式选的（档位不再生效），复审审核仍走快速档。
3. **硬限额**：把 `daily_token_limit` 设成 1000（比一轮正常消耗小），跑 `/bilearn once`。
   期望：本轮很快停下，`/bilearn recent` 里 runs 状态为 `budget_limited`，
   面板出现「预算跳过 N 次」；**视频没有被标成失败**（等明天额度重置后会重新处理）。
4. **软限额**：`daily_token_limit` 调回 0，`soft_token_limit` 设成 1000，再跑一轮。
   期望：摘要照常入库；审核/复审被跳过并在面板计入「预算跳过」。
5. **拒答重试**：把「备用模型」选成另一个模型，用会拒答的提示词不容易造——
   可跳过；若你手头有会拒答的模型，把它设成摘要模型，观察日志
   `model refusal on task=summary, retrying with fallback`，且不把拒答写进知识库。
6. **单次上限**：`single_call_token_cap` 设 100（很小）+ 配好备用模型 → 跑一轮。
   期望：摘要请求改用备用模型（日志/面板可见），不中断任务。
7. 验证完把限额改回 0（不限），避免以后正常学习被闸住。

## 2. 按需读（核心）

1. 找一个有字幕的热门视频，复制 BV 号：`/bilearn read BVxxxxxxxxxx`
   期望：返回标题 + UP + 链接 + 归类 + 3–7 条要点（带「视频摘要，非亲历」语气），
   面板 recent 里出现该 BV，状态 ingested。
2. 同一个 BV 再发一次（聊天里直接发链接，等 AI 调 `bilibili_read` 工具，或再跑一次命令）。
   期望：直接返回缓存摘要（不再调模型，速度明显快；命令返回里 `cached` 相关字样）。
3. 发一个明显无关的视频（如美食探店，而兴趣词只有科技）。
   期望：返回摘要但**不入库**（归类"其他"/off_topic，recent 里状态 excluded）。
4. 发一个乱写的链接（如 `/bilearn read 不是链接`）。
   期望：提示没有识别出 B 站视频，不崩。

## 2. 自动学习与配额

1. 兴趣词只留一个词，`daily_per_keyword` 设 1，每轮数量设 2，`/bilearn once`。
   期望：输出"运行完成 …… 入库=1 ……"（**不是**"未运行"，本轮修的 bug 就是这个误报）。
2. `/bilearn quota` 应显示该词今日 1/1；`/bilearn quota reset` 后计数清零。
3. 关键词用中文逗号分隔（如 `科技，数码`）保存后跑一轮。
   期望：按两个词分别搜索（本轮修之前会变成一个词"科技，数码"）。
4. 定时：把 `daily_start_hour` 改成未来 2 分钟的整点（如现在 14:03 就设 14:05），等触发。
   期望：runs 里多一条 cron 触发的运行。测完改回 1（或 -1 只手动）。

## 3. 查询

1. `/bilearn search 科技`：有入库时应返回条目（带相关度和 BV 链接）。
2. `/bilearn search`（不带参数）：返回概览（分区统计、汇总主题、最近入库）。
3. 把 Embedding 临时去掉（或用没绑知识库的会话）：再查应回退本地摘要库并注明。
4. `doc_name` 读全文：先 search 拿到文档名 → `/bilearn search` 不支持 doc 参数，
   让 AI 调 `bilibili_knowledge` 工具读全文（或面板看文档）。

## 4. 汇总

1. `consolidate_threshold` 临时改成 2，入库 2 条同类后跑 `/bilearn digest 科技`。
   期望：生成 `【汇总】…主题知识.md`，原视频文档按配置删除（默认删），SQLite 留档。
2. 再跑一次 `/bilearn digest 科技`：期望"没有需要汇总的文档"。
3. 恢复阈值为 10。

## 5. 审核

1. `/bilearn audit`：看统计行（通过/可疑/错误/未审）。
2. `/bilearn audit run 2`：审完后对应视频标 ok，可疑进列表（没有可疑是正常结果）。

## 6. 监控页

1. 打开页面：状态计数、运行记录时间线、视频状态表、汇总列表、审核统计都有数。
2. 兴趣词编辑：改一个词保存 → 刷新还在；写非法内容（如空行）不崩。
3. 健康检查：embedding/知识库/工具状态与实际一致；`subtitle_cooldown_seconds` 正常为 0。
4. 主题切换+自定义配色保存后刷新保持；无限模式开关打开会立即开始跑（慎点，烧 token），
   关掉后正在跑的收尾停下。
5. 手动试跑按钮：点后立即返回（不卡页面），runs 里出现新运行。

## 7. 兜底与稳定性

1. 把 `daily_per_keyword` 改成 `abc` 保存 → 跑 `/bilearn once`。
   期望：不崩，用默认值 3 跑（本轮修的脏配置守卫）。
2. 把 `daily_start_hour` 改成 `abc` → 重载插件。
   期望：插件正常加载（日志一句警告），定时不注册；改回数字后重载恢复。
3. 连续点 4 次以上"手动试跑"：期望最多排 3 个后台任务，多余的被拒绝（返回 started=false），
   不会无限堆积烧 token。
4. 重启 AstrBot：配置、审计库、已入库状态都在。

## 判定

失败时发我五样：
1. 聊天原文或命令原文（截图或复制）；
2. AstrBot 控制台里带 `Bili Learn` 的日志；
3. `/bilearn status` 的完整输出；
4. 面板配置截图（兴趣词/配额/开关页）；
5. 若是页面问题：浏览器 F12 → Console 的红色报错。

纯逻辑回归（不联网、不用上机）：

```text
python tests/test_core.py -v
```

应为 95 passed。集成测试（真实 AstrBot 框架 + 假 KB/假 B 站，需 `pip install astrbot`）：

```text
python tests/test_integration.py -v
```

应为 16 passed（另有 1 个真实 B 站网络探针，`BILI_LIVE=1` 才跑）。
